{
  "name": "gloss-io",
  "version": "0.2.0",
  "private": true,
  "scripts": {
    "dev": "npx @11ty/eleventy --serve",
    "build": "npx @11ty/eleventy",
    "start": "node server.js",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@11ty/eleventy": "^3.0.0",
    "dotenv": "^17.3.1"
  },
  "devDependencies": {
    "vitest": "^3.0.0"
  }
}
